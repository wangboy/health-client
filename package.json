{
  "name": "hc-react",
  "version": "0.1.0",
  "private": true,
  "proxy": "http://localhost:10000/",
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "dependencies": {
    "admin-on-rest": "^0.9.4",
    "cpx": "^1.5.0",
    "es6-promise": "^4.1.0",
    "isomorphic-fetch": "^2.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "copy": "cpx \"build/**\" ../hs/gateway/src/main/resources/static/ --watch"
  }
}
